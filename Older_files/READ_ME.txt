In the terminal, go to the folder containing all files and run
"python3.9 main.py --config_path=configs/PA_configs.json".
The parameters can be changed in the config files in the folder "configs/".
For each instance of the class, you need to create it by calling
a new .json file in config folder or use the same .json file and use "setattr". 
